---
type Props = {
	name: string;
	width?: string;
	height?: string;
};

const { name, width = '3rem', height = '3rem' } = Astro.props;
---

<svg width={width} height={height} aria-hidden="true">
	<use xlink:href={`#${name}`}></use>
</svg>
